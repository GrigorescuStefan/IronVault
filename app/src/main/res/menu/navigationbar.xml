<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="My Vault"
        android:id="@+id/myVault"
        android:icon="@drawable/vault"
        tools:ignore="HardcodedText" />
    <item android:title="Generator"
        android:id="@+id/generator"
        android:icon="@drawable/generator"
        tools:ignore="HardcodedText" />
    <item android:title="Scanner"
        android:id="@+id/scanner"
        android:icon="@drawable/scanner"
        tools:ignore="HardcodedText" />
    <item android:title="Settings"
        android:id="@+id/settings"
        android:icon="@drawable/gearsettings"
        tools:ignore="HardcodedText" />
</menu>